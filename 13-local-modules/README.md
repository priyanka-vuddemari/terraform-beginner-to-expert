A network module that should :

1. Create a VPC  with a given CIDR block 
2. allow the user  to provide the configuration for multiple subnets    
    a. The user should be mark a subnet private or public
    b. The user should be able to provide CIDR blocks
    c. The user should be able to provide AWS AZ

